# 安全披露

请勿在公开 Issue 中报告安全漏洞。

## 报告方式

通过 GitHub 仓库的 [Security advisory](https://github.com/haoyisun/team_dsh_plugins/security/advisories/new) 私下提交。

请包含：

- 受影响的插件或脚本
- 复现步骤
- 预期影响（例如任意代码执行、凭据泄露、数据损坏）

## 范围

本仓库的维护脚本会修改本机 DSH Profile，启用的工作区或外源插件都会以当前用户权限运行。外源注册表中的 npm 包、发布者和固定版本属于额外供应链信任边界。以下情况属于安全问题：

- 仓库代码可被诱导加载仓库外路径
- 外源插件同步绕过包名和精确版本校验，或把清单内容未经约束地拼接到 shell
- MCP 注册表允许提交本机绝对路径、认证信息或环境变量读取之外的任意 `!!js`
- 接入脚本会删除或覆盖用户数据，而不是仅移除本仓库接入
- 插件会把密钥写入仓库或日志

以下情况不属于安全漏洞：启用已审查的仓库插件或外源 npm 插件本身就意味着执行其代码；外源包上游自身的漏洞应同时向其维护者报告。

## 响应

维护者会确认收到报告，评估影响，并在修复发布后再公开细节。
