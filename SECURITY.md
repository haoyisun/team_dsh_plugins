# 安全披露

请勿在公开 Issue 中报告安全漏洞。

## 报告方式

通过 GitHub 仓库的 [Security advisory](https://github.com/haoyisun/team_dsh_plugins/security/advisories/new) 私下提交。

请包含：

- 受影响的插件或脚本
- 复现步骤
- 预期影响（例如任意代码执行、凭据泄露、数据损坏）

## 范围

本仓库的维护脚本会修改本机 DSH Profile，启用的工作区或外源插件都会以当前用户权限运行。外源 npm 包、发布者、目标版本、传递依赖和生命周期脚本属于额外供应链信任边界。以下情况属于安全问题：

- 仓库代码可被诱导加载仓库外路径
- 插件管理器接受 URL、Git spec、本地路径或版本范围，将用户输入拼接到 shell，绕过精确操作确认，或把 registry 凭据和本机路径返回浏览器
- MCP 管理器把凭据值或 credential ref 返回浏览器、写入日志，允许 Client 选择现有 ref，或未经确认执行已变化的 stdio、明文 HTTP 及携带凭据的连接
- 接入脚本会删除或覆盖用户数据，而不是仅移除本仓库接入
- 插件会把密钥写入仓库或日志

以下情况不属于安全漏洞：用户确认安装的外源 npm 插件及依赖按设计以当前用户权限执行；外源包上游自身的漏洞应同时向其维护者报告。

## 响应

维护者会确认收到报告，评估影响，并在修复发布后再公开细节。
